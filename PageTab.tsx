'use client'

interface PageTabProps {
    id: string;
    name: string;
  }
  
  export const PageTab: React.FC<PageTabProps> = ({ id, name }) => {
    return <></>; // The actual UI is handled in the PageTabsContainer
  };
  